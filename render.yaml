services:
  - type: web
    name: apna-youtube-dashboard
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python app.py  # or whatever command you use to run your Flask app
